# Runtime dependencies for DPDGen.
#
# gympn is not on PyPI.  Install it from git (the line below does that), or
# clone https://github.com/bpogroup/gympn and point GYMPN_PATH at the clone --
# see gympn_path.py.
#
#     pip install -r requirements.txt
#
# Tested on Python 3.12.

gympn @ git+https://github.com/bpogroup/gympn.git
simpn>=1.3.6
numpy>=2.0
pandas>=2.0
matplotlib>=3.8
openpyxl>=3.1
sortedcontainers>=2.4
pytest>=8.0
